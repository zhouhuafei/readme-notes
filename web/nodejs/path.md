# path模块
```
path.join(__dirname, '/config.js'); // 返回：全路径
path.resolve(__dirname, '/config.js') // 返回：/config.js

path.join(__dirname, 'config.js') // 返回：全路径
path.resolve(__dirname, 'config.js') // 返回：全路径
```
