# 微信开发者工具
* 编译模式，定格在某一个页面时，启动参数设置成：?id=3549。导致一直报错
    - 解决方案，去掉问号(?)。

# 跳转
* wx.navigateTo保留当前页面,跳转下一页
* wx.redirectTo关闭当前页面,跳转下一页
* 所以说用wx.navigateTo时, 能用wx.navigateBack
* wx.switchTab是唯一能跳转到 tabBar 页面
* wx.reLaunch是关闭所有页面,跳转到非tabBar的页面
